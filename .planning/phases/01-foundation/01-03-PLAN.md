---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - lib/preflight.cjs
autonomous: true
requirements: [ORCH-02, ORCH-08]

must_haves:
  truths:
    - "Pre-flight detects missing skills by checking SKILL.md on disk in known paths"
    - "Pre-flight detects missing CLIs (bd, br) via which/where check"
    - "Pre-flight checks for .reference/get-shit-done/ and offers setup if missing"
    - "Pre-flight returns blocking error (passed: false + exit 1) when deps missing — not a warning"
    - "Pre-flight asks user IDE choice on first run and stores in config.json"
    - ".reference/ is added to .gitignore automatically"
  artifacts:
    - path: "lib/preflight.cjs"
      provides: "Dependency checks, GSD reference setup, interactive installer output"
      exports: ["cmdPreflight"]
      min_lines: 100
  key_links:
    - from: "lib/preflight.cjs"
      to: "lib/core.cjs"
      via: "output(), error(), loadConfig(), saveConfig(), execGit(), pathExistsInternal()"
      pattern: "require.*lib/core"
    - from: "lib/preflight.cjs"
      to: ".reference/get-shit-done/"
      via: "checks existence, copies from ~/.claude/get-shit-done/ or clones"
      pattern: "get-shit-done"
---

<objective>
Build the pre-flight dependency checker that validates the entire environment before the pipeline starts. Checks skills, CLIs, GSD reference repo, and IDE preference. Returns structured JSON with all missing items so the orchestrator can offer interactive installation.

Purpose: CONTEXT.md mandates blocking pre-flight — the pipeline must not start with missing dependencies. Checking everything upfront prevents mid-pipeline failures that waste context and time.

Output: lib/preflight.cjs
</objective>

<execution_context>
@/Users/constantin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/constantin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — these modules already exist when Plan 03 executes -->

From lib/core.cjs:
```javascript
module.exports = {
  output(result, raw, rawValue),
  error(message, code),
  safeReadFile(filePath),
  execGit(cwd, args),
  pathExistsInternal(cwd, relPath),
  loadConfig(cwd),
  saveConfig(cwd, config),
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create lib/preflight.cjs — dependency checks and GSD reference setup</name>
  <files>lib/preflight.cjs</files>
  <action>
Build the pre-flight module. This is ralph-specific (GSD has no equivalent). Model the output pattern on GSD's JSON conventions.

**cmdPreflight(cwd, raw):**

Collect ALL missing dependencies into an array, then return structured result. Never exit early on first missing item — collect everything so user fixes all at once.

**Check categories (in order):**

1. **Required skills** — Check SKILL.md exists on disk:
   ```javascript
   const requiredSkills = [
     { name: 'ralph-tui-prd', purpose: 'PRD creation' },
     { name: 'ralph-tui-create-beads', purpose: 'Bead conversion (Go)' },
   ];
   ```
   Search paths (in order): `~/.claude/skills/{name}/SKILL.md`, then `{cwd}/.claude/skills/{name}/SKILL.md`. Use os.homedir() for ~.
   If not found in either location, add to missing: `{ type: 'skill', name, purpose, install_hint: 'Install via Claude Code skill system' }`.

2. **Optional CLIs** — Check if bd/br exist on PATH:
   ```javascript
   const optionalCLIs = [
     { name: 'bd', purpose: 'Go bead CLI', required: false },
     { name: 'br', purpose: 'Rust bead CLI', required: false },
   ];
   ```
   Check via `child_process.execSync('which {name}')` (wrapped in try/catch — catch means not found). On Windows, use `where` instead of `which`.
   If not found, add to missing but mark `required: false` — these are optional depending on bead format choice.

3. **GSD reference** — Check `.reference/get-shit-done/` exists in cwd:
   If missing, attempt to set up:
   a. Check if `~/.claude/get-shit-done/` exists (the plugin-installed copy).
   b. If yes, record `{ type: 'reference', name: 'get-shit-done', source: 'local', local_path: homeGsdPath }`. The orchestrator will handle the actual copy (preflight just reports what's available).
   c. If no local copy, record `{ type: 'reference', name: 'get-shit-done', source: 'none', install_hint: 'GSD must be installed via Claude Code plugin system first' }`.
   d. Read VERSION from the source if available, include as `version` field.

4. **.gitignore check** — Verify `.reference/` is in .gitignore:
   Read .gitignore, check if `.reference/` or `.reference` pattern exists. If not, add to `setup_actions`: `{ action: 'add_gitignore', pattern: '.reference/' }`.

5. **.planning/ directory** — Check if `.planning/` exists with required files (STATE.md, ROADMAP.md, PROJECT.md):
   If missing, add to `setup_actions`: `{ action: 'init_planning', missing_files: [...] }`.

6. **IDE preference** — Load config, check if `ide` is set:
   If null, add to `setup_actions`: `{ action: 'ask_ide', options: ['vscode', 'cursor', 'zed', 'neovim', 'other'] }`.

**Return format:**
```javascript
output({
  passed: missing.filter(m => m.required !== false).length === 0 && referenceOk,
  missing: missing,           // Array of missing dependencies
  setup_actions: actions,     // Array of setup tasks for orchestrator
  reference: referenceInfo,   // GSD reference status object
  config: { ide: config.ide },
}, raw);
```

If `passed` is false AND there are required missing items, also write to stderr (but still exit 0 — let the orchestrator decide to block). The orchestrator uses `passed: false` to block, not the exit code. Only use exit 1 for actual errors (malformed args, etc.).

**Important:** Do NOT actually perform installations or copies. Preflight is diagnostic only — it reports what's missing and what actions are available. The orchestrator handles the interactive installation flow.
  </action>
  <verify>
    <automated>node -e "const p = require('./lib/preflight.cjs'); console.log('exports:', Object.keys(p).join(', ')); console.assert(typeof p.cmdPreflight === 'function', 'cmdPreflight must be a function')" && node ralph-tools.cjs preflight --cwd . 2>&1 | node -e "d='';process.stdin.on('data',c=>d+=c);process.stdin.on('end',()=>{try{const r=JSON.parse(d);console.assert(typeof r.passed==='boolean','missing passed field');console.assert(Array.isArray(r.missing),'missing array');console.assert(typeof r.reference==='object','missing reference');console.log('Preflight output valid');console.log('passed:',r.passed);console.log('missing count:',r.missing.length)}catch(e){console.error('Parse failed:',e.message,d.substring(0,200));process.exit(1)}})"</automated>
  </verify>
  <done>lib/preflight.cjs checks skills, CLIs, GSD reference, .gitignore, .planning/ dir, and IDE preference. Returns structured JSON with passed boolean, missing array, setup_actions array, and reference status. Diagnostic only — does not perform installations.</done>
</task>

<task type="auto">
  <name>Task 2: Wire preflight into router and add .reference/ gitignore helper</name>
  <files>ralph-tools.cjs</files>
  <action>
**Update ralph-tools.cjs:**

1. Add require at top: `const preflight = require('./lib/preflight.cjs');`
2. Wire `preflight` command in switch/case: `case 'preflight': { preflight.cmdPreflight(cwd, raw); break; }`
3. Add a `setup-reference` command that performs the actual GSD reference copy:
   - Read preflight result to find GSD source location
   - If local_path available: create `.reference/` dir, use recursive copy (`fs.cpSync` on Node 16.7+, fallback to `cp -r` via execSync) from `~/.claude/get-shit-done/` to `.reference/get-shit-done/`
   - Add `.reference/` to .gitignore if not already present (read, check, append if needed)
   - Output `{ setup: true, reference_path: '.reference/get-shit-done/', version: versionString }`
4. Add a `setup-gitignore` command for adding patterns to .gitignore:
   - Takes a pattern argument
   - Reads .gitignore (or creates if missing), checks if pattern exists, appends if not
   - Output `{ added: true/false, pattern }`

These utility commands let the orchestrator perform setup actions reported by preflight.
  </action>
  <verify>
    <automated>node ralph-tools.cjs preflight --cwd . 2>&1 | head -1 | node -e "d='';process.stdin.on('data',c=>d+=c);process.stdin.on('end',()=>{try{JSON.parse(d);console.log('Valid JSON output')}catch{console.error('Invalid JSON');process.exit(1)}})"</automated>
  </verify>
  <done>preflight command wired in router. setup-reference copies GSD from ~/.claude/get-shit-done/ to .reference/. setup-gitignore adds patterns to .gitignore. Orchestrator can run preflight, then execute setup actions based on results.</done>
</task>

</tasks>

<verification>
1. `node ralph-tools.cjs preflight --cwd .` returns JSON with passed/missing/reference fields
2. `node ralph-tools.cjs setup-gitignore .reference/ --cwd .` adds pattern to .gitignore
3. `node ralph-tools.cjs setup-reference --cwd .` copies GSD reference (if ~/.claude/get-shit-done/ exists)
4. Running preflight after setup-reference shows reference as available
</verification>

<success_criteria>
- Preflight checks all dependency categories and returns structured JSON
- Missing skills detected by checking SKILL.md on disk
- GSD reference status reported with source location
- .gitignore management works for .reference/ pattern
- setup-reference copies GSD from plugin install location
- All output is JSON, no human-readable console messages
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
