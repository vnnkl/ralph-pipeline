---
phase: 15-marathon-mode-orchestration
plan: 2
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - MARATHON.md
autonomous: true
requirements:
  - MARA-03
  - MARA-05

must_haves:
  truths:
    - "After Convert phase, marathon presents a bead inventory table before stopping"
    - "User can drop individual beads from the queue (partial selection)"
    - "Rejection sends user back to Resolve phase, then re-runs Convert"
    - "YOLO auto-approves the bead review gate and accepts all beads"
    - "Marathon stops after bead review -- never dispatches execute or review"
    - "Config.marathon is cleaned up (set false) when marathon stops"
  artifacts:
    - path: "MARATHON.md"
      provides: "Bead inventory review gate (Step 8-9) appended to marathon skill"
      min_lines: 250
  key_links:
    - from: "MARATHON.md Step 8"
      to: ".beads/*.md"
      via: "Reads bead frontmatter to build inventory table"
      pattern: "beads.*frontmatter"
    - from: "MARATHON.md Step 9"
      to: ".planning/pipeline/resolve.md"
      via: "Deletes resolve + convert output files on rejection to force re-run"
      pattern: "rm.*resolve\\.md"
    - from: "MARATHON.md"
      to: "config.marathon"
      via: "Cleanup: sets marathon=false on stop"
      pattern: "config-set marathon false"
---

<objective>
Add the bead inventory review gate and YOLO integration to MARATHON.md -- the final steps that present the bead summary, allow partial selection, handle rejection (loop back to resolve), and clean up config on stop.

Purpose: MARA-03 requires the bead inventory review gate with partial selection and reject-to-resolve loop. MARA-05 requires YOLO auto-approval of the review gate.

Output: Steps 8-9 appended to MARATHON.md, plus config cleanup logic.
</objective>

<execution_context>
@/Users/constantin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/constantin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/15-marathon-mode-orchestration/15-CONTEXT.md
@.planning/phases/15-marathon-mode-orchestration/15-RESEARCH.md
@.planning/phases/15-marathon-mode-orchestration/15-01-SUMMARY.md

Reference:
@MARATHON.md (created by Plan 15-01 -- contains Steps 0-7)
@lib/frontmatter.cjs (extractFrontmatter for reading bead YAML)

<interfaces>
From lib/frontmatter.cjs:
- extractFrontmatter(content): Returns { frontmatter: {}, body: string, hasFrontmatter: boolean }
- Bead frontmatter fields typically: title, type, story_id, complexity (if present)

From .beads/*.md structure:
- Each bead file has YAML frontmatter with at least title/story fields
- Body contains implementation instructions and acceptance criteria
- Files named like: US-001-auth-flow.md, US-002-dashboard.md

Complexity estimation heuristic (from RESEARCH.md):
- Count lines in bead body: <20 = Low, 20-50 = Medium, >50 = High
- Or check frontmatter for complexity/size field if present
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add bead inventory review gate to MARATHON.md (Steps 8-9)</name>
  <files>MARATHON.md</files>
  <action>
Append Steps 8, 9, and 10 to the existing MARATHON.md (after Step 7 created by Plan 15-01). Replace the placeholder comment at the end of Step 7 with a reference to Step 8.

**Step 8: Bead Inventory Review Gate**

After phase 7 (convert) completes and is approved:

1. List all bead files:
```bash
ls {PROJECT_CWD}/.beads/*.md 2>/dev/null | sort
```

If zero beads found:
- Log: "No beads found after convert phase. Something went wrong."
- Clean up config: `config-set marathon false`
- Stop marathon with error message.

2. For each bead file, read its content and extract:
   - Bead name (from filename, strip .md)
   - Type: check if filename contains "bd" or "br" pattern, or read from frontmatter
   - Estimated complexity: count non-frontmatter lines. <20 = Low, 20-50 = Medium, >50 = High. If frontmatter has `complexity` field, use that instead.
   - Source: extract story_id from frontmatter or filename (e.g., "US-001")

3. Present the inventory table:

```
## Bead Inventory Review

| # | Bead | Type | Complexity | Source |
|---|------|------|------------|--------|
| 1 | US-001-auth-flow | bd | Medium | US-001 |
| 2 | US-002-dashboard | bd | High | US-002 |
...

**Total:** {N} beads
**Estimated execution time:** {estimate}h (based on {complexity_breakdown})
```

Time estimate heuristic: Low = 10min, Medium = 20min, High = 40min per bead. Sum and convert to hours.

4. Check YOLO mode:
```bash
node {RALPH_TOOLS} --cwd {PROJECT_CWD} config-get mode --raw
```

**If YOLO:**
- Auto-approve all beads
- Log: "YOLO mode: auto-approved all {N} beads"
- Skip to Step 10 (Marathon Complete)

**If NOT YOLO:**

Present options via AskUserQuestion:
- Header: "Bead Inventory Review"
- Question: "Review the bead inventory above. Choose how to proceed:"
- Options:
  1. **Approve all** -- Accept all beads and stop marathon
  2. **Drop beads** -- Specify which beads to exclude from execution
  3. **Reject** -- Return to Resolve phase to fix open questions, then re-run Convert

Handle responses:

**If "Approve all" (or "1"):** Proceed to Step 10 (Marathon Complete).

**If "Drop beads" (or "2"):**
- Ask: "Which beads should be dropped? Enter bead numbers (e.g., '2, 5, 7') or bead names:"
- Parse user response to identify which beads to remove
- Delete the specified bead files from `.beads/`:
  ```bash
  rm -f {PROJECT_CWD}/.beads/{BEAD_NAME}.md
  ```
- Log: "Dropped {N} beads. {remaining} beads remain."
- Re-display the updated inventory table (without dropped beads)
- Re-present the same 3 options (user may want to drop more or approve)

**If "Reject" (or "3"):** Proceed to Step 9 (Reject-to-Resolve Loop).

**Step 9: Reject-to-Resolve Loop**

When user rejects the bead inventory:

1. Reset resolve and convert phase outputs to force re-execution:
```bash
rm -f {PROJECT_CWD}/.planning/pipeline/resolve.md
rm -f {PROJECT_CWD}/.planning/pipeline/convert.md
```

2. Clear stale beads:
```bash
rm -f {PROJECT_CWD}/.beads/*.md 2>/dev/null
```

3. Log: "Returning to Resolve phase. Fix open questions, then Convert will re-run."

4. Re-enter the marathon phase loop starting from phase 6 (resolve). The scan-phases check will see resolve as incomplete (file deleted) and dispatch it, followed by convert (also deleted), which will produce new beads and return to Step 8 (bead review gate).

IMPORTANT: The loop re-entry point is Step 2 (Position Detection) -- scan-phases will naturally find phase 6 as the first incomplete phase and resume from there.

**Step 10: Marathon Complete**

After bead review is approved (or auto-approved by YOLO):

1. Log final summary:
```
## Marathon Planning Complete

All 7 planning phases finished. Bead inventory approved.

**Beads ready:** {N} beads in .beads/
**Bead format:** {format from config}
**Time budget:** {marathon_time_budget_hours}h (will start when execution begins)

To execute, run the standard pipeline:
  /ralph-pipeline

The pipeline will detect marathon-completed planning (phases 1-7 done),
skip to phase 8 (execute), and start the time budget at that point.
```

2. Clean up marathon-specific config:
```bash
node {RALPH_TOOLS} --cwd {PROJECT_CWD} config-set marathon false
```

Note: Do NOT clear `marathon_time_budget_hours` -- the standard pipeline needs this value to start the time budget when execution begins. Do NOT clear `bead_format` -- it persists for the project.

3. Clear auto_advance if set:
```bash
node {RALPH_TOOLS} --cwd {PROJECT_CWD} config-set auto_advance false
```

4. Stop. Marathon never dispatches execute or review phases.

**Config Cleanup on Any Stop:**
Whenever marathon stops for ANY reason (completion, rejection loop re-entry excepted, abort, error), ensure `config.marathon` is set to false. This prevents the standard pipeline from seeing stale marathon state. The reject loop does NOT clean up marathon (it re-enters the loop), but all terminal stops must clean up.
  </action>
  <verify>
    <automated>grep -c "Bead Inventory Review" MARATHON.md | xargs -I{} test {} -ge 1 && grep -c "config-set marathon false" MARATHON.md | xargs -I{} test {} -ge 1 && echo "PASS: Review gate and cleanup present" || echo "FAIL"</automated>
    Verify MARATHON.md contains: Step 8 (bead inventory review gate), Step 9 (reject-to-resolve loop with file deletion), Step 10 (marathon complete with config cleanup), YOLO auto-approve at review gate.
  </verify>
  <done>
    - Step 8 presents bead inventory table with name, type, complexity, source
    - YOLO mode auto-approves all beads at review gate
    - Non-YOLO mode offers approve/drop/reject options
    - Drop beads allows partial selection (delete specific bead files)
    - Reject deletes resolve.md + convert.md + .beads/*.md and loops back to phase 6
    - Step 10 shows completion summary with instructions to run standard pipeline
    - config.marathon set to false on completion
    - auto_advance cleared on stop
    - marathon_time_budget_hours preserved for standard pipeline
    - Marathon never dispatches phases 8 or 9
  </done>
</task>

</tasks>

<verification>
1. MARATHON.md contains Steps 8-10 after the Steps 0-7 from Plan 15-01
2. Bead inventory table reads .beads/*.md frontmatter
3. Complexity estimation uses line count heuristic
4. YOLO auto-approves review gate without user prompt
5. Non-YOLO offers approve/drop/reject with proper handling
6. Reject loop deletes resolve.md + convert.md + .beads/*.md
7. Reject loop re-enters at Step 2 (position detection finds phase 6 incomplete)
8. Config cleanup: marathon=false on all terminal stops
9. marathon_time_budget_hours preserved for execution phase
10. No phase 8/9 dispatch anywhere in the file
</verification>

<success_criteria>
- Bead inventory review gate presents actionable summary after Convert phase
- Partial bead selection (drop) works by deleting specific .beads/ files
- Reject-to-resolve loop correctly resets phase outputs and re-enters loop
- YOLO + marathon auto-approves everything including bead review
- Marathon stops cleanly with config cleanup and clear next-step instructions
</success_criteria>

<output>
After completion, create `.planning/phases/15-marathon-mode-orchestration/15-02-SUMMARY.md`
</output>
