---
phase: 10-cosmetic-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core.cjs
  - lib/frontmatter.cjs
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - ORCH-07

must_haves:
  truths:
    - "loadConfig returns auto_advance_started_at: null when key not set in config.json"
    - "ROADMAP.md phase checkboxes match actual completion status for all 10 phases"
    - "ROADMAP.md progress table matches STATE.md plan counts and completion dates"
    - "REQUIREMENTS.md traceability table maps TIME-01/TIME-04 to Phase 6 and ORCH-02 to Phase 7"
    - "spliceFrontmatter function body no longer exists in lib/frontmatter.cjs"
  artifacts:
    - path: "lib/core.cjs"
      provides: "loadConfig with auto_advance_started_at default"
      contains: "auto_advance_started_at"
    - path: "lib/frontmatter.cjs"
      provides: "Clean exports, no dead code"
    - path: ".planning/ROADMAP.md"
      provides: "Accurate phase/plan checkboxes and progress table"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Correct traceability mapping"
  key_links:
    - from: "lib/core.cjs"
      to: "config.json"
      via: "loadConfig defaults merge"
      pattern: "auto_advance_started_at"
---

<objective>
Fix loadConfig defaults, update stale ROADMAP checkboxes and progress table, verify traceability table accuracy, and remove dead spliceFrontmatter function body -- all info-severity cosmetic items closing INT-05, INT-06, and remaining tech debt.

Purpose: Eliminate stale documentation, prevent CONFIG_KEY_NOT_FOUND on auto_advance_started_at, and remove dead code before v1.0 ship.
Output: Four clean files with accurate state.
</objective>

<execution_context>
@/Users/constantin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/constantin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@lib/core.cjs
@lib/frontmatter.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add auto_advance_started_at to loadConfig defaults</name>
  <files>lib/core.cjs</files>
  <action>
In `lib/core.cjs`, inside the `loadConfig()` function (line ~109-125), add `auto_advance_started_at: null` to the `defaults` object. Place it after `phase_retry_count: 0` as the last entry.

Before:
```javascript
    phase_retry_count: 0,
  };
```

After:
```javascript
    phase_retry_count: 0,
    auto_advance_started_at: null,
  };
```

This ensures `config-get auto_advance_started_at` returns `null` instead of `CONFIG_KEY_NOT_FOUND` when the key has never been set. The SessionStart hook already reads config.json directly and guards with an if-check, so this is belt-and-suspenders.
  </action>
  <verify>
    <automated>node -e "const c = require('./lib/core.cjs'); const cfg = c.loadConfig('/tmp/nonexistent'); if (cfg.auto_advance_started_at !== null) { throw new Error('missing default'); } console.log('OK: auto_advance_started_at default is null');"</automated>
  </verify>
  <done>loadConfig returns auto_advance_started_at: null in defaults when config.json does not contain the key</done>
</task>

<task type="auto">
  <name>Task 2: Update stale ROADMAP.md checkboxes and progress table</name>
  <files>.planning/ROADMAP.md</files>
  <action>
Update `.planning/ROADMAP.md` to reflect actual completion status. All changes are checkbox/text updates -- no structural changes.

**Top-level phase checkboxes to change from `[ ]` to `[x]`:**
- Phase 1: `- [ ] **Phase 1:` -> `- [x] **Phase 1:`
- Phase 5: `- [ ] **Phase 5:` -> `- [x] **Phase 5:`
- Phase 6: `- [ ] **Phase 6:` -> `- [x] **Phase 6:`
- Phase 7: `- [ ] **Phase 7:` -> `- [x] **Phase 7:`
- Phase 8: `- [ ] **Phase 8:` -> `- [x] **Phase 8:`

**Plan-level checkbox updates:**
- Phase 5 plans: Change all three `- [ ] 05-01`, `- [ ] 05-02`, `- [ ] 05-03` to `- [x]`
- Phase 6 plan: Change `- [ ] 06-01` to `- [x] 06-01`
- Phase 7 plan: Change `- [ ] 07-01` to `- [x] 07-01`
- Phase 9 plans: Change both `- [ ] 09-01` and `- [ ] 09-02` to `- [x]`

**Phase 8 detail section updates:**
- Change `**Plans**: 0 plans (to be planned)` to `**Plans**: 2 plans`
- Add plan list after the Plans line:
```
- [x] 08-01-PLAN.md -- Replace preflight stub template + remove dead exports
- [x] 08-02-PLAN.md -- Replace clarify stub template with AskUserQuestion flow
```

**Phase 4 detail section correction:**
- ROADMAP shows "3 plans" and lists 3 entries, but 4 plans exist on disk (04-01 through 04-04)
- Update the Plans line from 3 to 4 plans
- Add the missing plan entry: `- [x] 04-04-PLAN.md -- Quality gate injection into bead prompts`

**Progress table updates (match STATE.md):**
| Phase | Current | Correct |
|-------|---------|---------|
| 4. Execution Layer | 3/3 | 4/4 |
| 5. Advanced Features | 0/3, Not started | 3/3, Complete, 2026-02-25 |
| 6. Time Budget Init | 0/1, Not started | 1/1, Complete, 2026-02-26 |
| 7. Preflight Cache | 0/0, Not started | 1/1, Complete, 2026-02-26 |
| 8. Tech Debt Cleanup | 0/0, Not started | 2/2, Complete, 2026-02-26 |

Phase 10 row should show `1/1, Not started` to reflect this plan existing but not yet complete.

Leave Phase 9 progress table row as-is (already shows 2/2 Complete).
  </action>
  <verify>
    <automated>grep -c '\- \[ \]' .planning/ROADMAP.md | xargs -I{} test {} -eq 1 && echo "OK: exactly 1 unchecked box (Phase 10)" || echo "FAIL: unexpected unchecked boxes"</automated>
  </verify>
  <done>All completed phases and plans show [x] checkboxes. Progress table plan counts and completion dates match STATE.md. Only Phase 10 remains unchecked.</done>
</task>

<task type="auto">
  <name>Task 3: Verify REQUIREMENTS.md traceability table accuracy</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Read `.planning/REQUIREMENTS.md` traceability table and verify these rows are correct:
- `TIME-01 | Phase 5, 6 | Complete`
- `TIME-04 | Phase 5, 6 | Complete`
- `ORCH-02 | Phase 1, 7 | Complete`

Research indicates these are ALREADY correct. If they match, no edit is needed -- just verify and confirm in the summary.

If any row is missing the Phase 6 or Phase 7 mapping, add it. For example, if TIME-01 shows only "Phase 5", change to "Phase 5, 6".

Also verify ORCH-07 maps to a phase that includes Phase 10 (since this phase addresses it). Current row shows `ORCH-07 | Phase 5, 10 | Complete` -- if it only shows Phase 5, update to Phase 5, 10.
  </action>
  <verify>
    <automated>grep 'TIME-01' .planning/REQUIREMENTS.md | grep -q 'Phase 5, 6' && grep 'TIME-04' .planning/REQUIREMENTS.md | grep -q 'Phase 5, 6' && grep 'ORCH-02' .planning/REQUIREMENTS.md | grep -q 'Phase 1, 7' && echo "OK: traceability correct" || echo "FAIL: traceability mismatch"</automated>
  </verify>
  <done>REQUIREMENTS.md traceability table correctly maps TIME-01/TIME-04 to Phase 5, 6 and ORCH-02 to Phase 1, 7 and ORCH-07 to Phase 5, 10</done>
</task>

<task type="auto">
  <name>Task 4: Remove dead spliceFrontmatter function from frontmatter.cjs</name>
  <files>lib/frontmatter.cjs</files>
  <action>
In `lib/frontmatter.cjs`, remove the dead `spliceFrontmatter` function and its section comment (lines ~233-243). Remove exactly:

1. The section comment: `// -- Splice -------------------------------------------------------------------`
2. The blank line after it
3. The JSDoc comment block (3 lines)
4. The function body (4 lines)

Before (lines 233-243):
```javascript
// -- Splice -------------------------------------------------------------------

/**
 * Extract frontmatter, merge updates immutably, reconstruct.
 * Returns full file content string.
 */
function spliceFrontmatter(content, updates) {
  const { frontmatter, body } = extractFrontmatter(content);
  const merged = { ...frontmatter, ...updates };
  return reconstructFrontmatter(merged, body);
}
```

After: These lines are deleted entirely. The `module.exports` block that follows remains unchanged.

The function was kept in Phase 8 "in case it's useful later" but Phase 10 success criteria explicitly requires its removal. It is not exported and has no callers.
  </action>
  <verify>
    <automated>! grep -q 'spliceFrontmatter' lib/frontmatter.cjs && echo "OK: spliceFrontmatter removed" || echo "FAIL: spliceFrontmatter still present"</automated>
  </verify>
  <done>spliceFrontmatter function body, JSDoc, and section comment are completely removed from lib/frontmatter.cjs. module.exports is unchanged.</done>
</task>

</tasks>

<verification>
1. `node -e "const c = require('./lib/core.cjs'); const cfg = c.loadConfig('/tmp/x'); console.log('auto_advance_started_at:', cfg.auto_advance_started_at);"` prints `auto_advance_started_at: null`
2. `grep -c '\- \[ \]' .planning/ROADMAP.md` returns 1 (only Phase 10)
3. `grep 'TIME-01' .planning/REQUIREMENTS.md` shows `Phase 5, 6`
4. `grep -c 'spliceFrontmatter' lib/frontmatter.cjs` returns 0
5. Existing tests still pass: `node --test test/`
</verification>

<success_criteria>
- loadConfig includes auto_advance_started_at: null in defaults
- ROADMAP.md phase checkboxes match actual completion (phases 1-9 checked, 10 unchecked)
- ROADMAP.md progress table plan counts and dates match STATE.md
- REQUIREMENTS.md traceability maps TIME-01/TIME-04 to Phase 6, ORCH-02 to Phase 7
- spliceFrontmatter function body fully removed from lib/frontmatter.cjs
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/10-cosmetic-cleanup/10-01-SUMMARY.md`
</output>
