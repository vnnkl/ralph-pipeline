---
phase: 08-tech-debt-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/clarify.md
autonomous: true
requirements:
  - ORCH-03
must_haves:
  truths:
    - "templates/clarify.md gathers project scope via AskUserQuestion instead of immediately writing completed: true"
    - "Clarify asks 3-4 questions: project name, description, primary stack, target platform, quality gates"
    - "If .planning/PROJECT.md has existing info, answers are pre-populated and presented for confirmation rather than asking from scratch"
    - "Clarify writes structured output to .planning/pipeline/clarify.md with both frontmatter fields and markdown body"
    - "Research template (templates/research.md) can parse the clarify output for project description, stack decisions, quality gates, scope boundaries"
    - "YOLO mode auto-generates reasonable answers from PROJECT.md context without prompting"
  artifacts:
    - path: "templates/clarify.md"
      provides: "Functional clarify template that gathers scope, stack, platform, quality gates via AskUserQuestion"
  key_links:
    - from: "templates/clarify.md"
      to: ".planning/pipeline/clarify.md"
      via: "Writes structured completion file consumed by research.md Step 1"
      pattern: ".planning/pipeline/clarify.md"
    - from: "templates/research.md"
      to: ".planning/pipeline/clarify.md"
      via: "Step 1 reads clarify output for PROJECT_CONTEXT"
      pattern: ".planning/pipeline/clarify.md"
---

<objective>
Replace the clarify stub template with a functional implementation that gathers project scope, stack, platform, and quality gates via AskUserQuestion, pre-populates from PROJECT.md when available, and writes structured output for downstream phases.

Purpose: Closes ORCH-03 tech debt -- clarify phase now gathers real project context instead of immediately writing completed: true. Research phase depends on this output.
Output: Functional templates/clarify.md that produces structured .planning/pipeline/clarify.md.
</objective>

<execution_context>
@/Users/constantin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/constantin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-tech-debt-cleanup/08-CONTEXT.md
@.planning/phases/08-tech-debt-cleanup/08-RESEARCH.md
@templates/clarify.md
@templates/research.md
@templates/resolve.md
@SKILL.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From ralph-tools.cjs CLI (relevant commands):
```bash
node ralph-tools.cjs config-get mode --raw   # Returns pipeline mode ("yolo" or other)
```

Downstream consumer -- templates/research.md Step 1 (lines 21-33):
```markdown
### Step 1: Read Context
Read the files listed in files_to_read above. Then read the clarify output for project scope and stack decisions:
.planning/pipeline/clarify.md

From the clarify output, extract:
- **Project description** -- what the project does
- **Stack decisions** -- languages, frameworks, libraries chosen
- **Quality gates** -- testing, coverage, linting requirements
- **Scope boundaries** -- what is in/out of scope

Store these as the PROJECT_CONTEXT to pass to each research agent.
```

Key insight: research.md uses natural language extraction from clarify.md. It does NOT parse frontmatter fields or use grep patterns. Use clear markdown section headers that are unambiguous to extract from.

AskUserQuestion pattern (from resolve.md and SKILL.md):
```
AskUserQuestion:
  Header: Short label, maximum 12 characters
  Question: The question with context
  Options:
    1. Option A -- description
    2. Option B -- description
    3. Let me explain my own answer
```

Existing template skeleton pattern:
```
<objective>...</objective>
<files_to_read>...</files_to_read>
<instructions>
<!-- YOLO mode: gate bypass handled by orchestrator (SKILL.md Step 6) -->
## Phase Name
### Step 1: Read Context
...
### Step N: Write Completion File
### Step N+1: Return Completion
## PHASE COMPLETE
</instructions>
<success_criteria>...</success_criteria>
```

Expected clarify.md output format (consumed by research.md):
```yaml
---
completed: true
project_name: "{name}"
primary_stack: "{language} + {framework}"
target_platform: "{platform}"
quality_gates: "{gates}"
---
## Project Scope

**Project:** {name}
**Description:** {one-line description}

**Stack:** {language} + {framework}
**Platform:** {platform}

**Quality Gates:**
- Test coverage: {threshold}
- Linting: {tool}

**Scope:** {brief scope statement from user}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace clarify stub template with functional implementation</name>
  <files>templates/clarify.md</files>
  <action>
Replace the entire contents of templates/clarify.md with a functional template following the existing template skeleton pattern.

The template structure must include these sections and steps:

**Objective section:** Execute clarify phase (phase 2 of 9). Gather project scope, stack choices, quality gates, and platform via interactive questions. Write structured output for downstream research and PRD phases.

**files_to_read section:** Include {{STATE_PATH}}, {{CONFIG_PATH}}, and {{PHASE_FILES}} template variables. Do NOT include ralph-tools.cjs.

**Instructions section:**

Step 1: Read Context -- Read files listed in files_to_read. Then check if .planning/PROJECT.md exists. If it does, read it and extract any existing project name, description, stack info, and platform info. Store extracted values as PRE_POPULATED defaults for Step 3.

Step 2: Check YOLO Mode -- Run `node ralph-tools.cjs config-get mode --raw`. Set YOLO_MODE flag.

If YOLO_MODE is true:
- Use PRE_POPULATED values from PROJECT.md if available
- For any missing values, generate reasonable defaults:
  - project_name: basename of {{CWD}}
  - description: "Project managed by ralph-pipeline"
  - primary_stack: attempt to detect from package.json, go.mod, Cargo.toml, or default to "Not specified"
  - target_platform: "web" (default)
  - quality_gates: "80% test coverage + linting"
- Log each auto-generated value: "YOLO mode: {field} = {value}"
- Skip all AskUserQuestion calls -- go directly to Step 4

If YOLO_MODE is false, proceed to Step 3.

Step 3: Gather Project Scope (3-4 questions via AskUserQuestion)

**Question 1: Project Name and Description**
- Header: "Project" (7 chars)
- If PROJECT.md had a name: "I found '{name}' in PROJECT.md. Is this correct, or would you like to change it?"
  - Options: 1. "{pre-populated name}" -- Confirm, 2. "Let me specify a different name"
- If no pre-populated name: "What is the project name and a one-line description?"
  - Options: 1. "Let me describe the project"
- After getting the name, ask for a one-line description if not already provided.

**Question 2: Primary Stack**
- Header: "Stack" (5 chars)
- Question: "What is the primary language and framework?"
- If PROJECT.md had stack info: Pre-populate as option 1
- Auto-detect from common files (package.json -> Node.js/TypeScript, go.mod -> Go, Cargo.toml -> Rust, requirements.txt -> Python) and offer as option
- Options: pre-populated/detected options + "Let me specify"

**Question 3: Target Platform**
- Header: "Platform" (8 chars)
- Question: "What is the target platform?"
- Options: 1. Web, 2. Mobile, 3. CLI, 4. Library, 5. Other -- Let me specify

**Question 4: Quality Gates**
- Header: "Quality" (7 chars)
- Question: "What test coverage and linting standards should the pipeline enforce?"
- Options: 1. "80% coverage + ESLint" -- Standard, 2. "90% coverage + strict linting" -- High bar, 3. "No quality gates" -- Skip enforcement, 4. "Custom" -- Let me specify

Step 4: Write Completion File

Write .planning/pipeline/clarify.md with YAML frontmatter and markdown body:

Frontmatter fields:
- completed: true
- project_name: "{name}"
- primary_stack: "{language} + {framework}"
- target_platform: "{platform}"
- quality_gates: "{gates description}"

Body (clear markdown section headers for research.md to parse):
```
## Project Scope

**Project:** {name}
**Description:** {one-line description}

## Stack

**Primary:** {language} + {framework}
**Detected from:** {source, e.g., "package.json", "PROJECT.md", "user input"}

## Platform

**Target:** {platform}

## Quality Gates

- Test coverage: {threshold}
- Linting: {tool/standard}

## Scope Boundaries

**In scope:** {brief scope from description and stack choices}
**Out of scope:** {anything explicitly excluded, or "Not specified"}
```

IMPORTANT: Use clear section headers (## Project Scope, ## Stack, ## Quality Gates, ## Scope Boundaries) so research.md Step 1 can extract each section by natural language understanding.

Step 5: Return Completion -- Return `## PHASE COMPLETE`

**success_criteria section:**
- Output file exists at .planning/pipeline/{{PHASE_SLUG}}.md with completed: true
- Output contains project name, stack, platform, and quality gates
- If PROJECT.md existed, pre-populated values were offered for confirmation
- Each question presented via AskUserQuestion with concrete options
- Research template (Step 1) can extract project description, stack decisions, quality gates, and scope boundaries from the output
  </action>
  <verify>
    <automated>grep -c "AskUserQuestion" templates/clarify.md | grep -q "^[1-9]" && grep -c "PHASE COMPLETE" templates/clarify.md | grep -q "^[1-9]" && grep -c "PROJECT.md" templates/clarify.md | grep -q "^[1-9]" && grep -c "YOLO" templates/clarify.md | grep -q "^[1-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>templates/clarify.md is a functional template that gathers project scope via AskUserQuestion (3-4 questions), pre-populates from PROJECT.md, handles YOLO mode with auto-generated defaults, and writes structured output with clear section headers parseable by research.md Step 1.</done>
</task>

</tasks>

<verification>
1. `grep "AskUserQuestion" templates/clarify.md` -- interactive questions present
2. `grep "PROJECT.md" templates/clarify.md` -- pre-population from existing project info
3. `grep "YOLO" templates/clarify.md` -- YOLO mode handling present
4. `grep "## PHASE COMPLETE" templates/clarify.md` -- completion return present
5. `grep "project_name\|primary_stack\|target_platform\|quality_gates" templates/clarify.md` -- all required fields present
6. `grep "## Project Scope\|## Stack\|## Quality Gates\|## Scope Boundaries" templates/clarify.md` -- section headers for research.md parsing
7. Template no longer contains "stub template" text
</verification>

<success_criteria>
- templates/clarify.md gathers project scope via AskUserQuestion (3-4 questions)
- Questions cover: project name+description, primary stack, target platform, quality gates
- If .planning/PROJECT.md exists, values are pre-populated and offered for confirmation
- YOLO mode auto-generates reasonable defaults without prompting
- Output written to .planning/pipeline/clarify.md with structured frontmatter and clear section headers
- Research template (research.md Step 1) can parse the output for project description, stack decisions, quality gates, scope boundaries
- Template no longer immediately writes completed: true
</success_criteria>

<output>
After completion, create `.planning/phases/08-tech-debt-cleanup/08-02-SUMMARY.md`
</output>
