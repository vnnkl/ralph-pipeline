---
phase: 04-execution-layer
plan: 04
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - templates/execute.md
  - templates/convert.md
autonomous: true
requirements: [CONV-01, CONV-02, CONV-03, CONV-04, EXEC-01, EXEC-02, EXEC-03, EXEC-04, REVW-01, REVW-02, REVW-03]
gap_closure: true

must_haves:
  truths:
    - "Execute template appends quality gate instructions to each bead invocation prompt so the bead agent is explicitly told to run tests and type checks"
    - "Convert template injects quality gate reminder into bead acceptance criteria during the validation/injection step"
    - "Pipeline still trusts bead agent exit code (no external quality check) per CONTEXT.md locked decision"
  artifacts:
    - path: "templates/execute.md"
      provides: "Quality gate instruction appended to bead invocation prompt"
      contains: "tests pass"
    - path: "templates/convert.md"
      provides: "Quality gate acceptance criteria injected into beads"
      contains: "tests pass"
  key_links:
    - from: "templates/execute.md"
      to: "bead agent invocation"
      via: "Appended prompt suffix with quality gate requirements"
      pattern: "tests.*type.check"
    - from: "templates/convert.md"
      to: ".beads/*.md"
      via: "Acceptance criteria injection in Step 6"
      pattern: "quality.gate"
---

<objective>
Close the EXEC-04 / ROADMAP Success Criterion 3 gap: ensure bead agents are explicitly instructed to enforce quality gates (tests pass, type checks pass).

Purpose: The execute template trusts bead exit codes but never instructs the bead agent to run tests or type checks. The convert template creates beads but doesn't inject quality gate requirements. This plan patches both templates so the bead agent receives explicit quality gate instructions -- closing the gap while honoring the CONTEXT.md locked decision that the pipeline trusts the bead agent's self-reported result.

Output: Patched `templates/execute.md` and `templates/convert.md`.
</objective>

<execution_context>
@/Users/constantin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/constantin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-execution-layer/04-CONTEXT.md
@.planning/phases/04-execution-layer/04-VERIFICATION.md
@templates/execute.md
@templates/convert.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patch execute.md to append quality gate instructions to bead invocation prompt</name>
  <files>templates/execute.md</files>
  <action>
Edit templates/execute.md to close the EXEC-04 gap. Two changes:

**Change 1: Add quality gate suffix to bead invocation in Step 4.**

In Step 4 (Sequential Bead Execution), after the line that shows the `cat ... | env -u CLAUDECODE claude -p` command pattern (around the stdin piping instruction), add a new instruction block:

Before piping the bead content to claude -p, append the following quality gate suffix to the bead prompt. This ensures the bead agent is explicitly instructed to enforce tests and type checks:

```
QUALITY_GATE_SUFFIX="

---
QUALITY GATES (mandatory before reporting success):
1. If the project has a test suite, run the relevant tests. Your work is not complete until tests pass.
2. If the project uses a typed language (TypeScript, Go, Rust, etc.), run the type checker. Your work is not complete until type checks pass.
3. If both tests and type checks fail, fix the issues before reporting success.
Your exit code MUST reflect the actual state: exit 0 only if your implementation is correct AND quality gates pass."
```

Update the execution command pattern from:
```bash
cat "{{CWD}}/.beads/{BEAD_NAME}.md" | env -u CLAUDECODE claude -p \
  --allowedTools "Read,Edit,Bash,Grep,Glob,Write" \
  --output-format json \
  --dangerously-skip-permissions
```

To a pattern that appends the quality gate suffix:
```bash
(cat "{{CWD}}/.beads/{BEAD_NAME}.md"; echo "$QUALITY_GATE_SUFFIX") | env -u CLAUDECODE claude -p \
  --allowedTools "Read,Edit,Bash,Grep,Glob,Write" \
  --output-format json \
  --dangerously-skip-permissions
```

Place the QUALITY_GATE_SUFFIX definition earlier in Step 4 (before the loop begins) so it is available for each bead invocation.

**Change 2: Update the success_criteria section.**

Replace the line:
```
- No external quality gates (pipeline trusts bead agent's self-reported result)
```

With:
```
- Quality gates enforced by instructing the bead agent to run tests and type checks before reporting success (pipeline trusts bead agent's self-reported result per CONTEXT.md decision, but explicitly instructs the agent to enforce quality gates)
```

IMPORTANT: Do NOT change anything else in the template. This is a surgical patch. The pipeline still trusts exit codes. No external quality check is added. The only change is that the bead agent now receives explicit instructions to run tests and type checks.
  </action>
  <verify>
    <automated>grep -c "QUALITY.GATE\|tests pass\|type check" templates/execute.md | xargs test 0 -lt</automated>
  </verify>
  <done>templates/execute.md appends quality gate instructions to each bead invocation prompt. The bead agent is explicitly told to run tests and type checks. Pipeline still trusts exit code (no external quality check added). Success criteria updated to reflect quality gate enforcement by delegation.</done>
</task>

<task type="auto">
  <name>Task 2: Patch convert.md to inject quality gate acceptance criteria into beads</name>
  <files>templates/convert.md</files>
  <action>
Edit templates/convert.md to add quality gate acceptance criteria injection alongside the existing frontend design injection in Step 6.

**Change 1: Add a new Step 6.5 (after Step 6, before Step 7) for quality gate injection.**

Add the following step:

```
### Step 6.5: Inject Quality Gate Acceptance Criteria

For ALL bead files (not just frontend beads), ensure quality gate requirements are present in acceptance criteria.

For each bead file in .beads/*.md:

1. Read the bead file content
2. Find the acceptance criteria section (same detection as Step 6)
3. If the acceptance criteria do NOT already contain "tests pass" or "type check" or "quality gate":
   - Append the following to the end of the acceptance criteria:

   ```
   - Quality gates: All relevant tests must pass and type checks must succeed before marking this bead complete.
   ```

4. Write the updated bead file back

Log: "Injected quality gate acceptance criteria into {N} beads."
```

This is additive -- it does not change Steps 1-6 or Steps 7-8. It runs after frontend injection and before the completion file write.

IMPORTANT: Do NOT rewrite the file. Add only the new step between Step 6 and Step 7. Keep all existing content intact.
  </action>
  <verify>
    <automated>grep -c "quality gate\|Quality Gate\|tests must pass\|type check" templates/convert.md | xargs test 0 -lt</automated>
  </verify>
  <done>templates/convert.md injects quality gate acceptance criteria into all beads during conversion. Every bead produced by the convert phase will include explicit quality gate requirements in its acceptance criteria, ensuring bead agents know to run tests and type checks.</done>
</task>

</tasks>

<verification>
1. templates/execute.md contains QUALITY_GATE_SUFFIX with instructions to run tests and type checks
2. templates/execute.md bead invocation pattern appends the quality gate suffix to bead content
3. templates/execute.md success_criteria reflects quality gate enforcement by delegation
4. templates/convert.md contains Step 6.5 injecting quality gate acceptance criteria into all beads
5. Neither template adds external quality checks (pipeline still trusts exit code)
6. No other template content is changed
</verification>

<success_criteria>
- EXEC-04 gap closed: bead agents receive explicit quality gate instructions via two mechanisms (execute template prompt suffix + convert template acceptance criteria injection)
- ROADMAP Success Criterion 3 satisfied: quality gates enforced per bead by delegation to bead agent with explicit instructions
- CONTEXT.md locked decision honored: pipeline trusts bead agent exit code, no external quality check added
- Both patches are surgical -- no unrelated changes to either template
</success_criteria>

<output>
After completion, create .planning/phases/04-execution-layer/04-04-SUMMARY.md
</output>
