---
phase: 09-integration-polish
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - SKILL.md
  - templates/convert.md
autonomous: true
requirements: [ORCH-05, ORCH-06, CONV-01, STATE-06]

must_haves:
  truths:
    - "SKILL.md Step 4 variable table uses PIPELINE_DISPLAY_NAME and PIPELINE_PHASE"
    - "SKILL.md Step 6 excerpt uses {pipeline_phase} slug, not {phase_name} from init"
    - "SKILL.md Step 6 skip path uses {pipeline_phase} slug"
    - "SKILL.md Step 6 excerpt requests 20 lines and handles missing/empty output"
    - "SKILL.md Step 2b prompts for bead_format when YOLO mode and bead_format is null"
    - "convert.md YOLO branch falls back to manual selection instead of failing hard on null bead_format"
  artifacts:
    - path: "SKILL.md"
      provides: "Updated variable table, excerpt/skip paths, YOLO bead_format prompt, excerpt improvements"
      contains: "PIPELINE_DISPLAY_NAME"
    - path: "templates/convert.md"
      provides: "YOLO branch with graceful fallback on null bead_format"
      contains: "Falling back to manual selection"
  key_links:
    - from: "SKILL.md Step 4"
      to: "lib/orchestrator.cjs fillTemplate"
      via: "Variable names in table must match what fillTemplate passes"
      pattern: "PIPELINE_DISPLAY_NAME|PIPELINE_PHASE"
    - from: "SKILL.md Step 2b"
      to: "templates/convert.md"
      via: "bead_format set in Step 2b is read by convert template YOLO branch"
      pattern: "bead_format"
    - from: "SKILL.md Step 6"
      to: "lib/orchestrator.cjs excerptFile"
      via: "excerpt path uses pipeline_phase slug from PIPELINE_PHASES, not phase_name from init"
      pattern: "pipeline_phase"
---

<objective>
Update SKILL.md variable table, excerpt/skip paths, excerpt line count, and "No output available" fallback. Add YOLO bead_format prompt to SKILL.md Step 2b. Update convert.md YOLO branch to fall back to manual selection instead of failing hard.

Purpose: Closes INT-03 (SKILL.md variable ambiguity), INT-04 (YOLO bead_format recovery), and the "User gate excerpt" flow from v1.0 audit.
Output: Updated SKILL.md with correct variable references and resilient excerpt handling. Updated convert.md with graceful YOLO fallback.
</objective>

<execution_context>
@/Users/constantin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/constantin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@SKILL.md
@templates/convert.md
@.planning/phases/09-integration-polish/09-01-SUMMARY.md

<interfaces>
<!-- After Plan 01, PIPELINE_PHASES looks like this: -->
```javascript
const PIPELINE_PHASES = [
  { id: 1, slug: 'preflight',  displayName: 'Pre-flight',  template: 'preflight.md',  gateOptions: [...] },
  { id: 2, slug: 'clarify',    displayName: 'Clarify',     template: 'clarify.md',    gateOptions: [...] },
  // ... etc
];
```

<!-- fillTemplate receives a variables object. SKILL.md Step 4 maps these: -->
<!-- Old: PHASE_NAME (capitalized), PHASE_SLUG (lowercase) -->
<!-- New (from Plan 01): PIPELINE_DISPLAY_NAME (from phase.displayName), PIPELINE_PHASE (from phase.slug) -->

<!-- SKILL.md Step 6 currently references {phase_name} which is ambiguous -->
<!-- After this plan: {pipeline_phase} refers to the slug from PIPELINE_PHASES -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update SKILL.md variable table, excerpt/skip paths, excerpt improvements, and YOLO bead_format prompt</name>
  <files>SKILL.md</files>
  <action>
Make these changes to SKILL.md:

**1. Step 4 variable table (around line 153-156):**

Replace the two variable table rows:
```
| `{{PHASE_NAME}}` | Capitalized phase name (e.g., "Research") |
| `{{PHASE_SLUG}}` | URL-safe phase name (e.g., "research") |
```

With:
```
| `{{PIPELINE_DISPLAY_NAME}}` | Pipeline phase display name from PIPELINE_PHASES (e.g., "Research", "PRD", "Pre-flight") |
| `{{PIPELINE_PHASE}}` | Pipeline phase slug from PIPELINE_PHASES (e.g., "research", "prd", "preflight") |
```

**2. Step 4 dispatch logic update:**

In the paragraph describing variable filling (around line 149-150), change:
```
Fill template variables using the `fillTemplate` function from `lib/orchestrator.cjs`:
```
Update the surrounding text to note that `PIPELINE_DISPLAY_NAME` comes from `phase.displayName` and `PIPELINE_PHASE` comes from `phase.slug` in the PIPELINE_PHASES array.

**3. Add Step 2b: YOLO Bead Format Check (new section after Step 2):**

Insert a new subsection between Step 2 and Step 3. Place it after the existing Step 2 content (after the `--auto` handling block, before `### Step 3: Status Banner`):

```markdown
### Step 2b: YOLO Bead Format Check

If mode is "yolo":
  Read bead_format: `node ralph-tools.cjs config-get bead_format --raw`
  If bead_format is null or empty:
    Ask via AskUserQuestion:
      - **Header:** "Bead Format (YOLO Setup)"
      - **Question:** "YOLO mode needs a bead format for the convert phase. Choose once for this project:"
      - **Options:**
        1. **bd** -- Go beads via /ralph-tui-create-beads
        2. **br** -- Rust beads via /ralph-tui-create-beads-rust
        3. **prd.json** -- JSON format via /ralph-tui-create-json
    Run: `node ralph-tools.cjs config-set bead_format {choice}`
    Log: "Bead format set to '{choice}' for this project."
```

**4. Step 6 excerpt path (around line 214):**

Change:
```
node ralph-tools.cjs excerpt .planning/pipeline/{phase_name}.md 10
```
To:
```
node ralph-tools.cjs excerpt .planning/pipeline/{pipeline_phase}.md 20
```

Where `{pipeline_phase}` is the slug from PIPELINE_PHASES (e.g., "research", "convert"), NOT the `phase_name` from init pipeline output.

**5. Step 6 "No output available" fallback (insert after the excerpt command):**

After the excerpt command, add:

```markdown
If the excerpt command fails (file not found) or returns an empty excerpt:
  Set excerpt to: "No output available for this phase."

Then present the gate with the excerpt and context-dependent options.
```

**6. Step 6 gate header (around line 220):**

Change:
```
## Phase {id}: {Name} Complete
```
To:
```
## Phase {id}: {pipeline_display_name} Complete
```

**7. Step 6 skip path (around line 231):**

Change:
```
- **skip** -- Mark phase as completed (write `.planning/pipeline/{phase_name}.md` with `completed: true` frontmatter), then proceed to Step 7.
```
To:
```
- **skip** -- Mark phase as completed (write `.planning/pipeline/{pipeline_phase}.md` with frontmatter only: `completed: true`), then proceed to Step 7.
```

Note: Per CONTEXT.md locked decision, skip path writes frontmatter only (completed: true), no body content.

**8. Step 3 Status Banner update:**

In the status banner template (around line 120), change:
```
**Phase:** {id} of 9 ({name})
```
To:
```
**Phase:** {id} of 9 ({pipeline_display_name})
```

Also in the auto-skip log line (around line 137):
```
Phase {id} ({name}): already complete, skipping.
```
To:
```
Phase {id} ({pipeline_display_name}): already complete, skipping.
```

**9. Update remaining {name} references in Steps 6 and 7:**

Search for remaining `({name})` patterns in SKILL.md Step 6 and Step 7 gate messages and log lines. Change all to `({pipeline_display_name})`. These include:
- Step 6 YOLO bypass log: `"YOLO mode: auto-approved phase {id} ({name})"` -> `"YOLO mode: auto-approved phase {id} ({pipeline_display_name})"`
- Step 6 failure auto-retry messages: `({name})` -> `({pipeline_display_name})`
- Step 7 messages: `({name})` -> `({pipeline_display_name})`

IMPORTANT: Do NOT rename `phase_name` in the Step 1 init output parsing section. That field describes the GSD dev-phase name (e.g., "Tech debt cleanup"), which is a distinct concept from the pipeline phase slug.
  </action>
  <verify>
    <automated>grep -n "phase_name\|{Name}\|{name}" SKILL.md | grep -v "phase_name.*init\|phase_name.*human\|phase_name.*STATE\|phase_name.*GSD\|phase_name.*name" | head -20</automated>
  </verify>
  <done>SKILL.md variable table uses PIPELINE_DISPLAY_NAME/PIPELINE_PHASE. Step 6 excerpt uses {pipeline_phase} with 20 lines and "No output available" fallback. Step 2b prompts for bead_format in YOLO mode. Skip path writes frontmatter only. All {name}/{Name}/{phase_name} references in Steps 3-7 use pipeline_display_name or pipeline_phase as appropriate. The `phase_name` field in Step 1 init parsing is preserved (GSD dev-phase name).</done>
</task>

<task type="auto">
  <name>Task 2: Update convert.md YOLO branch to fallback instead of failing</name>
  <files>templates/convert.md</files>
  <action>
In `templates/convert.md`, Step 2 (Present Bead Format Gate), update the YOLO branch:

Find the current YOLO null bead_format handling (around lines 43-45):
```
- If bead_format is NOT set: FAIL with error. In YOLO mode, bead_format must be pre-configured.
  - Write .planning/pipeline/convert.md with completed: false
  - Return: ## PHASE FAILED -- YOLO mode requires bead_format in config.json. Set via: node ralph-tools.cjs config-set bead_format bd
```

Replace with:
```
- If bead_format is NOT set: Fall back to manual selection.
  - Log: "YOLO mode: bead_format not set in config. Falling back to manual selection."
  - Present the AskUserQuestion bead format choice (same options as non-YOLO path below).
  - After selection, save to config: `node ralph-tools.cjs config-set bead_format {choice}`
  - Log: "Bead format set to '{choice}' for this project."
  - Use the selected format as CHOSEN_FORMAT and map to CHOSEN_SKILL.
```

This ensures that even if Step 2b in SKILL.md was somehow skipped (e.g., pipeline resumed mid-phase), the convert template gracefully recovers instead of hard-failing.
  </action>
  <verify>
    <automated>grep -c "PHASE FAILED.*bead_format" templates/convert.md && echo "FAIL: hard fail still present" || echo "PASS: hard fail removed"</automated>
  </verify>
  <done>convert.md YOLO branch falls back to manual selection on null bead_format. No hard fail on missing bead_format in YOLO mode.</done>
</task>

</tasks>

<verification>
1. SKILL.md Step 4 variable table contains `PIPELINE_DISPLAY_NAME` and `PIPELINE_PHASE` (no `PHASE_NAME` or `PHASE_SLUG`)
2. SKILL.md Step 6 excerpt path uses `{pipeline_phase}.md` with 20 lines
3. SKILL.md Step 6 has "No output available" fallback for missing/empty excerpt
4. SKILL.md Step 2b exists with YOLO bead_format prompt
5. SKILL.md Step 6 skip path writes frontmatter only
6. convert.md YOLO branch does NOT contain "PHASE FAILED" for null bead_format
7. convert.md YOLO branch falls back to manual selection
8. No remaining `{phase_name}` references in SKILL.md Steps 3-7 (only in Step 1 init parsing, which is correct)
</verification>

<success_criteria>
- SKILL.md variable table aligned with Plan 01 template rename
- Step 6 excerpt/skip uses pipeline phase slug, not init phase_name
- Excerpt shows 20 lines with "No output available" fallback
- YOLO bead_format prompted in Step 2b before any phase dispatch
- convert.md YOLO branch gracefully recovers from null bead_format
- INT-03, INT-04, and "User gate excerpt" flow issues all closed
</success_criteria>

<output>
After completion, create `.planning/phases/09-integration-polish/09-02-SUMMARY.md`
</output>
