---
phase: 12-yolo-time-budget-tracking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/execute.md
autonomous: true
requirements:
  - TIME-03
  - TIME-04

must_haves:
  truths:
    - "YOLO execute defaults to headless mode, not manual"
    - "Manual execution captures per-bead durations from result file timestamps"
    - "time-budget estimate uses actual bead duration data after any execution run"
    - "Duration is recorded for all beads including failed ones in headless mode"
  artifacts:
    - path: "templates/execute.md"
      provides: "YOLO headless default + manual duration capture"
      contains: "YOLO mode: defaulting to headless"
  key_links:
    - from: "templates/execute.md"
      to: "ralph-tools.cjs time-budget record-bead"
      via: "CLI call after manual result collection"
      pattern: "record-bead"
---

<objective>
Wire YOLO headless default and manual-mode duration capture into templates/execute.md.

Purpose: YOLO execute currently defaults to manual, preventing fully unattended runs. Manual execution never records bead durations, so time-budget estimates remain stuck on the 20-minute default. Both gaps close here.
Output: Updated templates/execute.md with three changes: (1) YOLO defaults to headless, (2) execution start time captured at phase entry, (3) manual-mode duration computation block added after result collection.
</objective>

<execution_context>
@/Users/constantin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/constantin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-yolo-time-budget-tracking/12-CONTEXT.md
@.planning/phases/12-yolo-time-budget-tracking/12-RESEARCH.md
@templates/execute.md
</context>

<interfaces>
<!-- Key CLI contract the executor needs -->

From ralph-tools.cjs:
```
node ralph-tools.cjs time-budget record-bead <duration_ms>
```
Accepts duration in milliseconds. Updates weighted running average in config.json (avg_bead_duration_ms, total_beads_executed). Already tested and working.

From bead result files (.claude/pipeline/bead-results/*.md):
```yaml
---
status: passed|failed
bead: BEAD_NAME
executed: 2026-02-27T12:00:00Z
---
```
The `executed` field is an ISO8601 timestamp written by Step 4 item 7 in execute.md.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Flip YOLO default to headless and capture execution start time</name>
  <files>templates/execute.md</files>
  <action>
Make two changes to templates/execute.md:

**Change 1: YOLO default flip (Step 2)**

Replace the current YOLO block in Step 2:
```
If mode is "yolo":
- Default to manual mode (locked decision from CONTEXT.md: user wants to be present for execution)
- Log: "YOLO mode: defaulting to manual execution (ralph-tui)"
- Skip AskUserQuestion, proceed to Step 3a (Manual Mode)
```

With:
```
If mode is "yolo":
- Default to headless mode
- Log: "YOLO mode: defaulting to headless execution"
- Skip AskUserQuestion, proceed to Step 3b (Headless Mode)
```

Key: Remove the "(locked decision from CONTEXT.md: user wants to be present for execution)" comment entirely -- per CONTEXT.md decision, this override is removed.

**Change 2: Execution start time (Step 1)**

After the "Report: Found {TOTAL_BEADS} beads ready for execution." line at the end of Step 1, add:

```
Record the execution start time for manual-mode duration tracking:
```bash
EXEC_START_TIME=$(date +%s%3N)
```
```

This goes BEFORE the Step 2 mode gate so it is available regardless of which path is taken.
  </action>
  <verify>
    <automated>grep -c "defaulting to headless" templates/execute.md | grep -q "1" && grep -c "locked decision from CONTEXT.md" templates/execute.md | grep -q "0" && grep -c "EXEC_START_TIME" templates/execute.md | grep -q "1" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - YOLO mode defaults to headless (Step 3b), not manual (Step 3a)
    - "locked decision from CONTEXT.md" comment removed
    - EXEC_START_TIME captured at end of Step 1 before mode gate
  </done>
</task>

<task type="auto">
  <name>Task 2: Add manual-mode duration capture after result collection</name>
  <files>templates/execute.md</files>
  <action>
Add a new section to templates/execute.md between Step 3a (Manual Mode) and Step 3b (Headless Mode). This block runs after manual-mode result collection, before aggregation.

Insert a new section "### Step 3a.1: Manual Mode Duration Tracking" after Step 3a item 6 ("Skip to Step 5 (Aggregate Results).") and before "### Step 3b: Headless Mode":

```markdown
### Step 3a.1: Manual Mode Duration Tracking

After manual result collection (Step 3a), compute per-bead durations from result file timestamps and record them.

1. List result files that have an `executed:` timestamp:
   ```bash
   ls .claude/pipeline/bead-results/*.md 2>/dev/null
   ```

2. If no result files exist, skip duration tracking entirely. (User chose "All passed" without files -- nothing to measure.)

3. If result files exist, extract the `executed:` timestamp from each file's YAML frontmatter. Sort the files by their `executed` timestamp (ascending).

4. Compute per-bead durations using consecutive timestamps:
   - For the first bead: `duration = executed_timestamp - EXEC_START_TIME`
   - For subsequent beads: `duration = executed_timestamp(N) - executed_timestamp(N-1)`

5. For each computed duration (in milliseconds), call:
   ```bash
   node ralph-tools.cjs time-budget record-bead $DURATION
   ```
   Skip any bead where the computed duration is zero or negative (indicates a timestamp issue).

6. Log: "Recorded duration for {N} beads from manual execution."
```

IMPORTANT: This duration tracking block applies to ALL manual execution, not just YOLO. Per CONTEXT.md Claude's discretion: "Whether to also enable manual duration capture for non-YOLO (if trivial to add)" -- the research recommends YES because the code is identical and it improves estimates for all users. The block sits in the manual path (Step 3a flow), which is reached regardless of YOLO mode.

Also update Step 3a item 6 from "Skip to Step 5 (Aggregate Results)." to "Proceed to Step 3a.1 (Manual Mode Duration Tracking)." so the flow is explicit.
  </action>
  <verify>
    <automated>grep -c "Manual Mode Duration Tracking" templates/execute.md | grep -q "1" && grep -c "record-bead" templates/execute.md | grep -qE "[2-9]" && grep -c "EXEC_START_TIME" templates/execute.md | grep -qE "[2-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - Step 3a.1 section exists between Step 3a and Step 3b
    - Duration computed from consecutive executed timestamps in result files
    - EXEC_START_TIME used as baseline for first bead
    - record-bead called for each computed duration
    - Graceful skip when no result files exist
    - Works for both YOLO and non-YOLO manual execution
    - Step 3a item 6 updated to point to Step 3a.1
  </done>
</task>

</tasks>

<verification>
1. `grep "defaulting to headless" templates/execute.md` -- confirms YOLO default flip
2. `grep "locked decision from CONTEXT.md" templates/execute.md` -- returns nothing (comment removed)
3. `grep "EXEC_START_TIME" templates/execute.md` -- appears at least twice (capture + usage in duration computation)
4. `grep "record-bead" templates/execute.md` -- appears at least twice (headless Step 4 item 8 + manual Step 3a.1)
5. `grep "Manual Mode Duration Tracking" templates/execute.md` -- new section exists
6. `grep "Step 3b" templates/execute.md` -- YOLO flow points to Step 3b (headless), not Step 3a (manual)
</verification>

<success_criteria>
- YOLO execute path defaults to headless mode, enabling fully unattended pipeline runs
- Manual execution path records per-bead durations via timestamp diffs from result files
- record-bead is called from both headless (existing Step 4 item 8) and manual (new Step 3a.1) paths
- No changes to lib/time-budget.cjs, ralph-tools.cjs, or any other file -- purely template edits
- Headless duration capture for non-YOLO remains unchanged (already works)
- Duration recorded for all beads including failed ones in headless mode (already works -- Step 4 item 8 before item 10)
</success_criteria>

<output>
After completion, create `.planning/phases/12-yolo-time-budget-tracking/12-01-SUMMARY.md`
</output>
